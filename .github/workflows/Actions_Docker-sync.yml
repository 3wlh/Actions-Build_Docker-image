name: Actions Docker Sync CNB

on:
  workflow_dispatch:
    inputs:
      image:
        description: 选择要同步的镜像
        required: true
        type: choice
        options:
          - 3wlh/photopea
          - alpine
          - alpine:3.18

      platform:
        description: 选择要同步的目标架构
        required: true
        default: "all"
        type: choice
        options:
          - all
          - linux/amd64,linux/arm64
          - linux/amd64
          - linux/arm64

jobs:
  docker-sync:
    uses: ./.github/workflows/call/Call_Docker-Sync-CNB.yml
    secrets: inherit
    with:
      image: ${{ inputs.image }}
      platform: ${{ inputs.platform }}
